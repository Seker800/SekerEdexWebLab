# SekerEdexWebLab 开发规则

## 开始之前

- 先阅读本文件、`docs/architecture.md` 与 `docs/visual-parity.md`。
- 改动架构边界、第三方来源或验收标准时，同时更新对应文档或 ADR。
- 当前阶段是可行性验证；不要未经证据就扩展成账号系统或通用组件库。

## 产品边界

- Phase 0 的唯一视觉基准是 eDEX-UI 2.2 默认 `tron` 主题、QWERTY 键盘并运行
  `neofetch` 的上游官方截图；不得自行混入 `blade`、`horizon`、`disrupted` 等主题风格。
- “一比一复刻”首先指参考画布上的布局、比例、文字层级、颜色、线框、间距和可见状态；
  有意偏离必须记录在 `docs/visual-parity.md`，不能用个人审美直接替换。
- 目标是复刻 eDEX-UI 的全屏运行感、交互反馈和信息密度，不是制作一张相似截图。
- 桌面端必须保持单屏工作台。
- 终端风格不是目标本身。终端、文件系统、监控、键盘和声音必须产生可观察联动。
- 本项目是非官方实现，不得暗示获得 eDEX-UI 或其作者背书。
- 不照搬 SekerEagle 的技术路线；SekerEagle 仅作为文档与维护流程参考。

## 来源与许可证

- 允许研究 eDEX-UI 的公开行为、截图和 GPLv3 源码。
- 复制或改写任何第三方代码、CSS、音频、图标前，必须先确认许可证并记录文件级来源。
- 禁止提交来源不明的素材、抓取音效或第三方截图。
- 没有明确许可证的第三方仓库只能作为行为参考，不能复制实现。

## 工程要求

- 技术路线遵循 `docs/adr/0003-web-technology-route.md`；没有测量证据不得替换框架或渲染层。
- 先建立最小可运行垂直切片，再扩展主题系统或部署能力。
- 动态模块使用真实浏览器或站点数据；演示数据必须明确标为模拟数据。
- 输入、音频、动画和会话状态通过类型化命令、事件与窄接口协作，禁止组件互相直接操纵内部 DOM。
- React 只管理可声明的界面状态；xterm.js、Three.js、Canvas 与 Web Audio 必须封装为可销毁的渲染适配器。
- 所有持续动画、采样和定时任务必须由统一调度器管理，不得在组件中散落独立循环。
- 首次播放必须由用户手势触发，并提供持续可见的声音控制。
- 必须支持键盘操作、`prefers-reduced-motion` 和无声音完成核心导航。
- 桌面实现使用固定 1920×1080 逻辑画布；其他桌面视口只能等比缩放并居中留边，不得重排
  或拉伸。移动端单独定义降级模式。
- 不为了截图相似牺牲可读性、可恢复状态或浏览器安全边界。

## 验证要求

- 每个里程碑至少运行格式检查、静态检查、单元测试和生产构建；命令在工具链确定后写入维护指南。
- 涉及视觉或动画的变更必须提供目标视口截图或录屏证据。
- 涉及输入联动的变更必须验证实体键盘、屏幕键盘、鼠标和触摸中的适用路径。
- 涉及性能的变更必须记录测试设备、视口、采样方式和结果，不能只写“很流畅”。
- 可行性阶段完成前，必须逐条关闭 `docs/visual-parity.md` 的必选验收项。

## 自动复刻系统

- 实现代码改动前还要阅读 `NORTH_STAR.md`、`ARCHITECTURE.md` 与
  `docs/VISUAL_NORTH_STAR.md`。
- Codex 可以提出和实现修复，只有可执行门禁可以判定场景通过。
- 目标证据、验收阈值、判定器和运行产物不可为了通过测试而修改。
- 每个发现的反例都要加入回归语料；修复共享原因，不添加页面特例。
- 浏览器采集、比较、判定和 Codex 执行保持独立接口，并在进程边界使用类型化数据。
- 同一工作区不能同时存在两个写入者；凭据不得进入提示词、日志、截图或提交文件。
- 修复后运行 `npm run check`、`npm test`、`npm run demo`、`npm run app:verify`
  与 `npm run replicate:verify`。
- Review 时必须拦截无界重试、从不可信目标内容拼接 Shell 命令，以及把浏览器观察、
  代码修改和通过判定混合在同一模块的实现。

## Commit 与 Push

- 提交类型使用中文：`新增`、`修复`、`重构`、`清理`、`测试`、`文档`。
- 每个提交只包含一个完整、可解释的行为变化，生成物与源声明一起提交。
- 使用 `git commit -s` 添加 DCO `Signed-off-by`。
- Agent 在验证通过后可自行创建提交，无需再次征求许可。
- 已配置并确认目标远端时，Agent 可自主执行普通 push，无需再次征求许可。
- 禁止 force push、`git push --all`、`git push --mirror`，也不得擅自创建或替换远端。
- 没有远端、认证失败或分支保护拒绝时，保留本地提交并清楚报告，不得绕过保护。
- 提交或 push 前检查 `git status --short`，不得覆盖、回滚或夹带无关改动。
